version: '3'

services:
  nginx-container:
    image: ghcr.io/oscarzhou-portainer/httpd:2.4-alpine
    # image: ghcr.io/oscarzhou-portainer/httpd:alpine
    container_name: nginx-cont
    ports:
      - "${PORT:-81}:80"
    labels:
      - branch="main"
      - version=5
