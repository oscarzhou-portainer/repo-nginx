version: '3'

services:
  nginx-container:
    image: registry.internal.services.oscarchou.com/nginx:alpine
    container_name: "nginx-private-image-relative-path"
    volumes:
      - ../relativepath/html:/usr/share/nginx/html
    ports:
      - "${PORT:-8122}:80"
