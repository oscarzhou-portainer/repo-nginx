version: '3'

services:
  httpd-1:
    image: registry.internal.services.oscarchou.com/httpd:latest
    
  httpd-3:
    image: ghcr.io/jamescarppe/nginx:latest
    # image: ghcr.io/oscarzhou-portainer/httpd:latest

# In order to reproduce the issue, we need to configure another ghcr private registry in Portainer
